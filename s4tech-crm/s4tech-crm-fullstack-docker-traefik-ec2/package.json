{
  "name": "s4tech-crm-fullstack",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm --prefix client run dev\" \"npm --prefix server run dev\"",
    "client": "npm --prefix client run dev",
    "server": "npm --prefix server run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}